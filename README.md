# Chat-Koi-Gelo
Vanish Chat is a real-time temporary messaging web application built on the MERN stack (MongoDB, Express.js, React.js, Node.js) with Socket.IO for live communication. A user's chat history exists only as long as they keep the browser tab open. Once the tab is closed, all their messages vanish permanently.
